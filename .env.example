# Example environment variables for ZenAuth server
#
# Copy this file to `.env` and edit values as needed.
# NOTE: `.env` is ignored by git (see .gitignore). Do not commit real secrets.

# Required: JWT signing key (use a long random string in production)
ZENAUTH_SECRET_KEY=change-me-to-a-random-secret

# Required: public origin of this auth server
# Example: https://auth.example.com
ZENAUTH_AUTH_SERVER_ORIGIN=http://localhost:8000

# Required: SQLAlchemy DSN for the server DB
# Example (SQLite local file):
ZENAUTH_SERVER_DSN=sqlite+pysqlite:///./zenauth.db

# Cookie settings (recommended for HTTPS deployments)
# ZENAUTH_SECURE=true
# ZENAUTH_SAMESITE=lax

# Optional: refresh window (seconds)
# ZENAUTH_SERVER_REFRESH_WINDOW_SEC=300

# Optional: bootstrap an initial admin user (development/demo only)
# ZENAUTH_SERVER_BOOTSTRAP_ADMIN=false
# ZENAUTH_SERVER_BOOTSTRAP_ADMIN_USER=admin
# ZENAUTH_SERVER_BOOTSTRAP_ADMIN_PASSWORD=change-me-now

# Optional: audit logging
# When true, audit logs may include token timestamps (token_iat/token_exp)
# ZENAUTH_AUDIT_INCLUDE_TOKEN_TIMESTAMPS=false

# Optional: CORS (server)
# For production, prefer restricting to your WebApp origin(s) or disabling CORS.
# (Default is disabled: empty origins)
# ZENAUTH_SERVER_CORS_ALLOW_ORIGINS=http://localhost:9000
# ZENAUTH_SERVER_CORS_ALLOW_CREDENTIALS=true

# Optional: CSRF (server)
# Recommended if using cookie auth from browsers.
# ZENAUTH_SERVER_CSRF_PROTECT=true
# ZENAUTH_SERVER_CSRF_TRUSTED_ORIGINS=http://localhost:9000
